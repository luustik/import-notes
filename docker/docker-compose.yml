volumes:
  pg_new_system:

services:

  new_system_db:
    image: postgres:15.1-alpine
    container_name: new-system-db
    environment:
      - POSTGRES_USER=${NEW_DB_USERNAME}
      - POSTGRES_PASSWORD=${NEW_DB_PASSWORD}
      - POSTGRES_DB=new_system_db
    volumes:
      - pg_new_system:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d new_system_db" ]
      interval: 1s
      timeout: 5s
      retries: 5
      start_period: 5s